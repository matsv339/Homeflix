//If condition is correct insert tamplet.
angular.module('movieModule').directive('myProfile', function () {
	return {
		restrict: 'AE',
		scope: true,
		templateUrl: '<%= asset_path("last_watched.html.erb")%>',
		controller: 'ProfileController',
		link: function(scope, elem, attrs, ctrl) {
			
		},
		// replace: true
	}
});