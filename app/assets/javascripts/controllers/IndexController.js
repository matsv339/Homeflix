angular.module('sessionModule').controller('IndexController', function($scope, Sessions) {	
	
/*	Sessions.show(function(data) {
 		$scope.allt = data;
 		$scope.current_user = data.name;
 		$scope.current_user_id = data.uid;
 		console.log($scope.current_user);
	console.log($scope.current_user_id);
	console.log($scope.allt);
 	});
*/
	


	
 	
	
	});
